DATABASE_URL=postgresql://nursery_app:nursery_app@localhost:5432/nursery_os

PORT=3001

# Development-only: fixed tenant used by FixedTenantProvider until
# JWT-based tenant resolution replaces it. Never a client-supplied value.
DEV_TENANT_ID=

JWT_ACCESS_TOKEN_PRIVATE_KEY=
JWT_ACCESS_TOKEN_PUBLIC_KEY=
JWT_ACCESS_TOKEN_TTL=15m
JWT_REFRESH_TOKEN_TTL=30d
