import { EnrollmentWizard } from '@/components/enrollment-wizard/enrollment-wizard';

export const metadata = { title: 'Enroll Child · Nursery OS' };

export default function EnrollChildPage() {
  return <EnrollmentWizard />;
}
