import { BillingRunForm } from '@/components/configuration/billing-runs/billing-run-form';

export const metadata = { title: 'Trigger Billing Run · Nursery OS' };

export default function NewBillingRunPage() {
  return <BillingRunForm />;
}
