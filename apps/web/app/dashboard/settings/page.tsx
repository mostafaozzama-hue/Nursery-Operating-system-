import { PagePlaceholder } from '@/components/layout/page-placeholder';

export const metadata = { title: 'Settings · Nursery OS' };

export default function SettingsPage() {
  return <PagePlaceholder title="Settings" />;
}
